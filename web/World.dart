import 'Player.dart';
import 'Enemy.dart';

class World {
  Player player;
  List<Enemy> enemies;
  //Image background;
  double boundaryX;
  double boundaryY;

  World(boundaryX, boundaryY, player, enemies);

  
}